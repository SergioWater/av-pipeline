# AV Telemetry Pipeline — Python dependencies
# Install into a virtual env:
#   python -m venv .venv && source .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt

# --- Telemetry simulator (Kafka producer) ---
confluent-kafka==2.5.3

# --- Stream processing and Delta Lake storage ---
# PySpark requires a compatible Java runtime (Java 17 is recommended here).
pyspark==3.5.3
delta-spark==3.2.1
