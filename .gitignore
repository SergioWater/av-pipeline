# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments (we use conda 'avpipe', but ignore any stray venvs too)
.venv/
venv/
env/
ENV/
.python-version

# ============================================================
# Jupyter / IPython (in case you add notebooks later)
# ============================================================
.ipynb_checkpoints/
*.ipynb_checkpoints

# ============================================================
# Spark / Hadoop / Delta — local state and downloaded artifacts
# ============================================================
spark-warehouse/
metastore_db/
derby.log
*.crc
.spark_history/

# Ivy + Maven caches Spark drops next to runs
.ivy2/
.m2/

# Local checkpoint dirs if you ever point them at the project
checkpoints/
**/checkpoints/

# ============================================================
# Logs
# ============================================================
*.log
logs/

# ============================================================
# Secrets / credentials (NEVER commit these)
# ============================================================
.env
.env.*
!.env.example
*.pem
*.key
credentials
credentials.json
secrets.yaml

# ============================================================
# Editor / IDE
# ============================================================
.vscode/
.cursor/
.idea/
*.swp
*.swo
*~

# ============================================================
# OS junk
# ============================================================
.DS_Store
Thumbs.db
desktop.ini

# ============================================================
# Docker — don't commit container state, only the compose file
# ============================================================
# (docker-compose.yml IS committed; only ignore local overrides)
docker-compose.override.yml

# ============================================================
# Project-specific
# ============================================================
# Old / dead code being kept locally for reference
consumer/old_spark_stream.py

# Anything dumped to /tmp during local runs (sanity guard)
tmp/

# Local sandbox / scratch
scratch/
notes_private.md